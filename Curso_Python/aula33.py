#Tuples
    #Armazenar mais de uma informcao em variavel
    #Manter a sequencia de dados em uma variavel
    #Nao podem ser alteradas (Immutable)

cores_list = ['rosa', 'azul']
cores_tuples = ('rosa', 'azul')

print(cores_list)
print(cores_tuples)